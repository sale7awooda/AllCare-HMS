
{
  "name": "allcare-hms-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && mkdir -p ../backend/public && cp -r dist/* ../backend/public && cd ../backend && npm install && npm rebuild better-sqlite3",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": "18.x"
  }
}
